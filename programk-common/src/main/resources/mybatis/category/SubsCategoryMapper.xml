<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright (c) 2016 KT, Inc.
  ~ All right reserved.
  ~ This software is the confidential and proprietary information of KT
  ~ , Inc. You shall not disclose such Confidential Information and
  ~ shall use it only in accordance with the terms of the license agreement
  ~ you entered into with KT.
  ~
  ~ Revision History
  ~ Author              Date                  Description
  ~ Seo Jong Hwa        2016 . 6 . 22
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kt.programk.common.repository.category.SubsCategoryMapper">
    <resultMap id="BaseResultMap" type="SubsCategory">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="created" jdbcType="TIMESTAMP" property="created"/>
        <result column="modified" jdbcType="TIMESTAMP" property="modified"/>
        <result column="restriction" jdbcType="VARCHAR" property="restriction"/>
        <result column="cp_id" jdbcType="INTEGER" property="cpId"/>
        <result column="enabled" jdbcType="VARCHAR" property="enabled"/>
        <result column="deploy" jdbcType="VARCHAR" property="deploy"/>
        <result column="cp_label" jdbcType="VARCHAR" property="cpLabel"/>
        <result column="count" jdbcType="VARCHAR" property="count"/>
    </resultMap>

    <!-- 검색시 where 조건 -->
    <sql id="whereSearch">
		WHERE  0 = 0		
		<if test='userAuth != "CPA" and cpId != null and cpId != 0'>
            AND cp_id = #{cpId}
        </if>
		<if test='name != null and name != ""'>
            AND LOWER(name) LIKE '%'|| LOWER(#{name}) ||'%'
        </if>
		<if test='restriction != null and restriction != ""'>
            AND restriction = #{restriction}
        </if>
	</sql>
	
	<sql id="tableCategory"> 
		<if test='userAuth == "CPA"'>
		(
			SELECT id, name, created, modified, restriction, cp_id, enabled
			FROM subs_category
			WHERE restriction = 'all'
			UNION ALL
			SELECT id, name, created, modified, restriction, cp_id, enabled
			FROM subs_category
			WHERE restriction = 'owner'
			AND cp_id = #{cpId}
		) AS t1
		</if>
		<if test='userAuth != "CPA"'>
    	subs_category AS t1
		</if>
    </sql>
    
	<sql id="columnDeploy"> 
		<if test='userAuth == "CPA"'>
		(
			CASE WHEN (SELECT true FROM deploy_subs_category WHERE cp_id = #{cpId} AND cate_id = t1.id) THEN 'Y'
			ELSE 'N' 
			END
		) AS deploy
		</if>
		<if test='userAuth != "CPA"'>
    	(
			CASE WHEN (SELECT true FROM deploy_subs_category WHERE cp_id = t1.cp_id AND cate_id = t1.id) THEN 'Y'
			ELSE 'N' 
			END
		) AS deploy
		</if>
    </sql>
    
    <sql id="columnCpLabel"> 
    	(
			SELECT label FROM cp WHERE id = t1.cp_id
		) AS cp_label
    </sql>
    
    <sql id="columnCount"> 
    	(
			SELECT count(*) FROM aiml_subs WHERE cate_id = t1.id
		) AS count
    </sql>
	
	<!-- 카운트 조회 -->
	<select id="countAll" parameterType="SubsCategory" resultType="java.lang.Integer" >
        select count(*) 
        FROM 
        <include refid="tableCategory" />
        <include refid="whereSearch" />
    </select>
    
    <select id="countByName" parameterType="SubsCategory" resultType="java.lang.Integer" >
        select count(*) from subs_category
        WHERE 0 = 0
        AND cp_id = #{cpId}
        AND name = #{name}
        <if test='id != null and id != 0'>
            AND id != #{id}
        </if>
    </select>

    <!-- 샘플 상세 조회 -->
    <select id="selectByPrimaryKey" parameterType="SubsCategory"
            resultMap="BaseResultMap">
          SELECT id, name, created, modified, restriction, cp_id, enabled,
          	<include refid="columnDeploy"/>
          FROM subs_category AS t1
          WHERE id = #{id} ;
    </select>

    <!-- 샘플 리스트 조회 -->
    <select id="selectList" parameterType="SubsCategory"
            resultMap="BaseResultMap">
          SELECT id, name, created, modified, restriction, cp_id, enabled,
          		<include refid="columnDeploy"/>,
			<include refid="columnCpLabel"/>,
			<include refid="columnCount"/>		
          FROM 
          <include refid="tableCategory" />
          <include refid="whereSearch" />
          ORDER BY restriction ASC, id DESC
          offset ${firstRecordIndex} limit ${recordCountPerPage};
    </select>

    <!-- 파일업로드시 카테고리 아이디 조회 -->
    <select id="selectListByName" parameterType="SubsCategory"
            resultMap="BaseResultMap">
        <![CDATA[
            SELECT id, name, created, modified, restriction, cp_id, enabled
            FROM subs_category
            WHERE name = #{name}
            AND cp_id = #{cpId}
        ]]>
    </select>

    <!-- 샘플 저장 -->
    <insert id="insert" parameterType="SubsCategory">
        <selectKey keyProperty="id" resultType="int" order="BEFORE">
            select nextval('seq_subs_category')
        </selectKey>
        <![CDATA[
        INSERT INTO subs_category(id, name, created, modified, restriction, cp_id, enabled)
        VALUES (#{id}, #{name}, now(), now(), #{restriction}, #{cpId}, #{enabled});
        ]]>
    </insert>

    <!-- 샘플 삭제 -->
    <delete id="deleteByPrimaryKey" parameterType="SubsCategory">
        <![CDATA[
          DELETE FROM subs_category
          WHERE id = #{id};
        ]]>
    </delete>

    <!--샘플 수정 -->
    <update id="updateByPrimaryKeySelective" parameterType="SubsCategory">
        UPDATE subs_category
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="restriction != null">
                restriction = #{restriction},
            </if>
            <if test="cpId != null">
                cp_id = #{cpId},
            </if>
            <if test="enabled != null">
                enabled = #{enabled},
            </if>
            modified = now()
        </set>
        WHERE id = #{id};
    </update>
    
    <!-- 전체 삭제 -->
    <delete id="deleteAll" parameterType="SubsCategory">
    	 DELETE FROM subs_category
         WHERE cp_id = #{cpId}
    </delete>

</mapper>
